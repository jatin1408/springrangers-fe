import { BooleanLocale, StringLocale } from 'yup/lib/locale';

export declare type props = {
  localProps?: any;
  label?: string;
  name: string;
  placeholder?: string;
  multiple?: BooleanLocale;
  className?: string;
  options?: any;
};
